<%= form_for [:admin,@slider], html: { multipart: true } do |f| %>
  <% if @slider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slider.errors.count, "error") %> prohibited this slider from being saved:</h2>

      <ul>
      <% @slider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :descriptiong %><br>
    <%= f.text_area :descriptiong %>
  </div>
  <div class="field">
    <%= f.label :published %><br>
    <%= f.check_box :published %>
  </div>
  <div class="field">
    <%= f.label :published_from %><br>
    <%= f.date_select :published_from %>
  </div>
  <div class="field">
    <%= f.label :published_to%><br>
    <%= f.date_select :published_to %>
  </div>

  <div class="field">
    <%= f.label :descriptiond %><br>
    <%= f.text_area :descriptiond %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
